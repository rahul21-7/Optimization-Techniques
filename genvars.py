import numpy as np
import pandas as pd
import random as random

size_of_population = random.randint(1,1000)

n = int

initial_random_population = np.random.rand(size_of_population,n)

# print((initial_random_population*10).astype(int))


